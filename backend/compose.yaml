services:
  api:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    volumes:
      - ai-data:/app/data
    restart: unless-stopped

volumes:
  ai-data:
